export enum Role {
  ADMIN = "ADMIN",
  USER = "USER"
}

export enum CommunityVisibility {
  PUBLIC = "PUBLIC",
  PRIVATE = "PRIVATE"
}

export enum ReportStatus {
  OPEN = "OPEN",
  REVIEWED = "REVIEWED",
  RESOLVED = "RESOLVED"
}
